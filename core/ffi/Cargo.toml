[package]
name = "missioncontrol-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
workflow = { path = "../workflow" }
knowledge = { path = "../knowledge" }
runtime = { path = "../runtime" }
serde_json = "1.0"
